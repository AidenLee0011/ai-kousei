__pycache__/
*.pyc
build/
dist/
*.egg-info/
web/rules/
# Fetched source documents are not redistributed: the scripts fetch them, the
# repository keeps only measurements and citations.
sources/raw/
sources/ref/
sources/reports/*.txt
sources/reports/_tmp.pdf

# demo recordings are build artifacts of the live page
docs/media/*.webm

.env*
.npmrc
